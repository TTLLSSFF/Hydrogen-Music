# 歌单页面选择功能更新

## 修改内容

### 1. 功能变更
- **下载按钮** → **选择按钮**
  - 点击"选择"按钮后立即进入选择模式
  - 同时展开右侧菜单显示操作选项

### 2. 新增功能
点击"选择"按钮后，会向右滑出以下操作选项：
- **下载** - 下载所选歌曲
- **添加到歌单** - 将所选歌曲添加到指定歌单
- **添加到播放列表** - 将所选歌曲添加到当前播放列表
- **从歌单中删除** - 从当前歌单中删除所选歌曲

### 3. UI/UX 改进
- 使用新的选择图标（列表样式图标）
- 子选项平滑向右滑出动画
- 动画效果与歌曲选中背景高亮保持一致
- 字体大小与"选择"按钮保持统一（15px）
- 每个子选项依次延迟出现，创造流畅的视觉效果

### 4. 动画细节
- 使用 `cubic-bezier(0.14, 0.91, 0.58, 1)` 缓动函数
- 子选项从左侧淡入并滑出（translateX + opacity）
- 每个选项递增延迟：0.05s, 0.1s, 0.15s, 0.2s
- 总动画时长：0.32s

### 5. 功能修复
- ✅ 修复"添加到歌单"功能失效问题
- ✅ 修复"添加到播放列表"功能失效问题
- ✅ 实现批量操作支持（多选歌曲后统一操作）

## 技术实现

### 新增状态变量
```javascript
const selectionMenuExpanded = ref(false)  // 控制菜单展开状态
```

### 新增函数
- `enterSelectionMode()` - 进入选择模式
- `downloadSelected()` - 下载所选歌曲
- `addSelectedToPlaylist()` - 添加到歌单
- `addSelectedToPlayerList()` - 添加到播放列表
- `deleteSelectedFromPlaylist()` - 从歌单中删除

### CSS类名
- `.operation-selection-wrapper` - 选择按钮容器
- `.operation-selection` - 选择按钮
- `.selection-menu` - 菜单容器
- `.selection-menu-expanded` - 菜单展开状态
- `.selection-menu-item` - 菜单项
- `@keyframes selection-item-slide-in` - 滑入动画

## 使用说明

1. 打开任意歌单或专辑页面
2. 点击"选择"按钮进入选择模式
3. 选项菜单会从右侧滑出，显示4个操作选项
4. 点击列表中的歌曲进行多选
5. 选择完成后，点击对应的操作按钮：
   - 点击"下载"：选择音质后下载所选歌曲
   - 点击"添加到歌单"：打开歌单列表，选择目标歌单
   - 点击"添加到播放列表"：直接添加到当前播放队列
   - 点击"从歌单中删除"：从当前歌单移除所选歌曲
6. 也可以点击"全选"快速选择所有歌曲
7. 点击"取消"退出选择模式

## 注意事项
- "从歌单中删除"功能仅在查看歌单时可用
- 批量删除会一次性删除所有选中的歌曲
- 添加到播放列表会按选择顺序依次添加
- 所有操作完成后会自动退出选择模式
