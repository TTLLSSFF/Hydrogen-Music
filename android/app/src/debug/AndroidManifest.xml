<?xml version="1.0" encoding="utf-8"?>
<!-- HTTPS validation build: API traffic uses the public HTTPS gateway; only the
     local WebView origin may use cleartext. Release keeps the platform policy. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/validation_network_security" />
</manifest>
